version: 2

jobs:

  build:
    docker:
      - image: cgarretty/confero
    steps:
      - checkout
      - run: echo 'Run the tests....'
      - run:
          name: Setup AWS credentials
          command: |
            mkdir ~/.aws && printf "[profile eb-cli]\naws_access_key_id = ${AWS_ACCESS_KEY_ID}\naws_secret_access_key = ${AWS_SECRET_ACCESS_KEY}" > ~/.aws/config
      - deploy:
          filters:
            branches:
              only:
                - master
          name: Deploy to Elastic Beanstalk Environment
          command:
            eb init project-confero
            eb deploy confero-env
