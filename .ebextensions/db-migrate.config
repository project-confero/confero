container_commands:
  01_migrate:
    command: "docker exec -i -t `docker ps --no-trunc -q | head -n 1` /bin/bash python manage.py migrate"
    leader_only: true
option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: confero.settings
